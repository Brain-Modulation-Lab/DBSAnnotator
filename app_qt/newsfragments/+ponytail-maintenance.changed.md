Internal maintenance and slimming: de-duplicated the session and longitudinal report exporters into a shared `report_common` module, consolidated the JSON config managers behind shared `user_data` helpers, removed dead code, and trimmed CI gates. Linux installers no longer bundle `docx2pdf` (PDF export continues to use LibreOffice there). No user-facing behavior change to reports.
