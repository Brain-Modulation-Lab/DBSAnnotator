# Documentation build dependencies.
#
# Deliberately a plain requirements file rather than a dependency group in a
# pyproject: the app this documents is written in Dart, so there is no Python
# project at the repository root, and inventing one purely to feed Read the Docs
# would add a second thing to maintain for no benefit.
#
# Versions are pinned to a compatible range rather than left open, because
# .readthedocs.yaml sets fail_on_warning — an unpinned Sphinx minor release that
# introduces a new warning would silently stop the docs publishing.
sphinx>=8.1,<10
sphinx-rtd-theme>=3.0
sphinx-copybutton>=0.5.2
myst-parser>=4.0
